{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for all Inkfish windows (single document and project)",
  "windows": ["main", "viewer-*", "project-*"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:window:allow-set-title",
    "opener:default",
    {
      "identifier": "opener:allow-open-path",
      "allow": [{ "path": "**" }, { "path": "/**" }]
    },
    "dialog:default",
    "dialog:allow-confirm"
  ]
}
